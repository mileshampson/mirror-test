stages:
  - build
  - release

tests:
  stage: build
  script:
    - ls
  tags:
    - push_test

merge:
  stage: release
  only:
    - master
  script:
    - git push --quiet github
  tags:
    - push_test
